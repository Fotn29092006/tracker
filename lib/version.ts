// Bump on every shippable change so the running build is identifiable in-app
// (Profile footer) — lets us confirm a deploy actually reached the device
// instead of guessing about PWA cache.
export const APP_VERSION = '9';
